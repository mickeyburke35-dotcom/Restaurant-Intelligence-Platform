# Session 25 Promise-to-Proof

This table narrows product claims to what the current app and repository support. It does not claim customers, traction, market size, live provider integrations, production readiness, or generated PDF/CSV exports. `PROJECT_HANDOFF.md` was requested but is not present in the workspace, so it is not used as evidence.

| Claim | Evidence | Status | Action Taken |
| --- | --- | --- | --- |
| The current app is a Next.js, React, TypeScript, Tailwind, Prisma, and Zod project. | `package.json`, `app/`, `components/`, `tailwind.config.ts`, `prisma/schema.prisma`, and validation schemas in `lib/`. | Supported. | Kept as an implementation claim. |
| The data model is designed around agency-scoped restaurant intelligence records. | `prisma/schema.prisma` includes `agencyId` on restaurants, locations, review sources, reviews, insights, reports, scheduled jobs, audit logs, competitors, and competitor observations. | Supported at schema level. | Kept, but not expanded into production-readiness or migration-completion claims. |
| The app resolves active agency membership and role before protected business access. | `middleware.ts`, `lib/auth/session.ts`, `lib/auth/membership.ts`, and `lib/request-context.ts`. | Supported. | Narrowed from full email/password and Google auth to the implemented membership-backed session flow. |
| Restaurants and locations can be managed within tenant scope. | `app/api/restaurants/*`, `lib/restaurants.ts`, `lib/locations.ts`, and matching UI routes under `app/restaurants/`. | Supported. | Kept as current app functionality. |
| Review source records can be listed, created, edited, and archived without live scraping. | `app/api/restaurants/[id]/review-sources/route.ts`, `app/api/review-sources/[reviewSourceId]/route.ts`, and `lib/review-sources.ts`. | Supported. | Kept, with live provider collection and scraping claims removed. |
| Approved public review CSV import exists with validation, deduplication, and disallowed contact columns. | `app/api/reviews/import/preview/route.ts`, `app/api/reviews/import/confirm/route.ts`, and `lib/review-imports.ts`. | Supported. | Narrowed to approved public CSV import only. |
| Reviews can be viewed with tenant-scoped filtering, searching, sorting, pagination, and summary counts. | `app/reviews/page.tsx`, `components/review-dashboard.tsx`, and `lib/reviews.ts`. | Supported. | Kept as current app functionality. |
| Current runtime AI uses Google Gemini for review summaries and draft insight generation. | `lib/ai/gemini.ts`, `lib/ai/review-summary.ts`, `lib/ai/review-insights.ts`, and `app/api/ai/review-summary/route.ts`. | Supported. | Narrowed OpenAI references to placeholder or roadmap context; current runtime claim is Gemini only. |
| Draft AI insights are generated from selected tenant-scoped approved reviews and store source evidence. | `lib/insights.ts` validates selected reviews against active agency, restaurant, optional location, and approved review source, then creates `Insight` and `InsightSourceReview` rows. | Supported. | Kept as current app functionality. |
| AI insight output is constrained to draft status until a human approves or rejects it. | `reviewInsightStatus` in `lib/insights.ts` approves or rejects only `DRAFT` insights and records reviewer, timestamp, notes, and audit log. | Supported. | Kept as current app functionality. |
| Reports are stored report snapshots built from approved insights only. | `app/api/reports/route.ts`, `app/reports/page.tsx`, `app/reports/[reportId]/page.tsx`, and `lib/reports.ts` create `READY` report records with `approvedOnly: true` and stored sections. | Supported. | Narrowed from PDF/CSV export language to stored approved-insight report snapshots; generated files are not claimed. |
| Demo lead capture writes to Supabase and then notifies Zapier with a minimal payload. | `app/api/demo/leads/route.ts` validates email, inserts `email` and `source`, then sends `email`, `source`, and `created_at` to the Zapier webhook. | Supported. | Narrowed Supabase and Zapier usage to demo lead capture only. |
| Competitor entities exist in the schema, but live competitor tracking is not implemented as current app behavior. | `prisma/schema.prisma` defines competitor models; current route scan shows no competitor API or UI route. | Narrowed. | Removed live competitor monitoring claims from current-app proof. |
| The app does not prove production readiness, customer traction, market statistics, or live review-provider integrations. | `package.json` has `npm test` as a placeholder; README and PRD describe planned provider integrations and exports; current code scan shows no live review-provider sync jobs. | Not claimed. | Kept unsupported claims out of this proof artifact. |
