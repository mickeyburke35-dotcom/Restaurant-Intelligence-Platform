# Session 25 Skill Risk Audit

Scope: Restaurant Intelligence Platform governance, security, and IP review only. This audit uses the current repository docs and source surface. `PROJECT_HANDOFF.md` was requested but is not present in the workspace, so no claims depend on it.

| Tool | Provenance | Maintainer | Executes code? | Data access | Memory/file write? | Worst case if compromised | Do we still trust it? |
| --- | --- | --- | --- | --- | --- | --- | --- |
| GitHub | Project workflow references GitHub push, PRs, and post-push security audits in `AGENTS.md`, `README.md`, and `tasks/todo.md`. | GitHub, owned by Microsoft. | Yes, through GitHub Actions, app integrations, branch operations, and repository automation when configured. | Repository source, issues, PRs, comments, metadata, and any configured CI secrets. | Yes, it can store repository history, branches, PR metadata, comments, releases, and CI artifacts. | Source tampering, secret exposure through Actions, malicious PR changes, or loss of repository integrity. | Yes, conditionally. Use least-privilege app access, branch protection, reviewed PRs, and the required post-push security audit. |
| Supabase | Current app references `SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY`; implemented only in `POST /api/demo/leads` for demo lead insertion. | Supabase Inc. | Server-side platform executes database, auth, storage, edge, and PostgREST behavior; current repo uses REST insert only. | Current app sends demo lead `email` and `source` to `public.leads`; it does not store tenant restaurant intelligence data there. | Yes, Supabase writes records to its hosted database and logs service activity. | Service-role key compromise could expose or modify Supabase project data and leak demo leads. | Yes, conditionally for the narrow server-side lead capture use case. Do not use it for tenant business data without an approved architecture change. |
| Prisma | `package.json` depends on `prisma` and `@prisma/client`; `prisma/schema.prisma` defines tenant-scoped models. | Prisma, maintained by Prisma Data, Inc. | Yes, Prisma CLI generates client code and runs migrations; Prisma Client executes database queries. | PostgreSQL data reachable through `DATABASE_URL`, including agencies, memberships, restaurants, locations, reviews, insights, reports, and audit logs. | Yes, it can write generated client output, migrations, and database records. | Compromised dependency or migration could read, corrupt, or exfiltrate tenant data. | Yes, with pinned lockfile review, migration review, tenant-scope query review, and server-only database credentials. |
| Google AI Studio / Gemini | Current AI runtime uses `GOOGLE_AI_API_KEY` and `lib/ai/gemini.ts` for review summaries and draft insights. | Google. | No direct app code execution, but it processes prompts and returns model output used by server code. | Selected review text and metadata sent in prompts; request body sets `store: false` in current Gemini calls. | It does not write repo files. App code stores generated insight output, model, confidence, and source evidence in PostgreSQL. | Prompt data exposure, unsupported AI output, model drift, or malicious prompt-injection content influencing draft insights. | Yes, conditionally. Keep prompts evidence-only, validate JSON with Zod, require source review IDs, store traceability, and require human approval. |
| Zapier | Current app references `ZAPIER_LEAD_WEBHOOK_URL`; implemented as a fire-and-forget webhook after Supabase accepts a demo lead. | Zapier, Inc. | Yes, Zapier can run downstream automation configured outside this repo. | Current payload is demo lead `email`, `source`, and `created_at`. | It stores task history and can write to connected downstream apps; it does not write repository files. | Lead data could be sent to the wrong destination, spammed, or exposed through a misconfigured Zap. | Yes, conditionally. Keep payload minimal, webhook URL server-only, and avoid sending tenant restaurant intelligence data. |
| Next.js | `package.json` depends on Next.js; app routes, API routes, middleware, and pages live under `app/`. | Vercel and the Next.js open-source project. | Yes, it executes server route handlers, middleware, build code, and client bundles. | All request data and server-side route data reachable through implemented pages and API routes. | Yes, build artifacts and runtime cache may be written; route handlers can write through services. | Framework or configuration compromise could expose tenant data, bypass auth, or ship malicious client/server code. | Yes, with dependency review, strict server/client boundaries, route-level tenant checks, and build verification. |
| React | `package.json` depends on React and React DOM; UI components live under `app/` and `components/`. | Meta and the React open-source project. | Yes, browser-executed client UI code. | Data rendered into the browser for the active user's permitted scope. | No repository writes at runtime; browser state and forms hold user-entered data. | XSS or compromised UI dependency could expose rendered tenant data or submit unintended actions. | Yes, with React escaping, careful form handling, no unsafe HTML patterns, and role-gated server APIs. |
| Tailwind | `package.json`, `tailwind.config.ts`, and `app/globals.css` define Tailwind styling. | Tailwind Labs and the Tailwind open-source project. | Yes, at build time through PostCSS/Tailwind processing. | Source class names and styling config only; no business data by design. | Yes, compiled CSS/build outputs can be generated. | Supply-chain compromise could alter UI, obscure warnings, or inject unsafe CSS into builds. | Yes. Risk is lower than data-layer tools, but keep lockfile review and avoid unreviewed plugin additions. |
| Zod | `package.json` depends on Zod; used across API, auth, review import, insight, report, and AI response validation. | Zod open-source maintainers. | Yes, validation library code runs in server and client contexts where imported. | Request bodies, query params, CSV rows, and AI JSON response shapes passed for validation. | No direct file writes; validation results can influence database writes by app code. | Compromise could accept unsafe input, reject valid controls, or bypass validation assumptions. | Yes. Keep schemas strict, review validation boundaries, and do not treat validation as a substitute for tenant checks. |
| OpenAI API placeholder | `PRD.md`, `ARCHITECTURE.md`, `AGENTS.md`, and `.env.example` reference OpenAI; current runtime AI code uses Gemini instead. | OpenAI. | No current app execution found for OpenAI API calls. If implemented later, model calls process prompts and return output. | Currently none at runtime. Future use would likely include selected review evidence and AI prompt context. | No current runtime writes. Future app code could store OpenAI-generated insights. | Future misuse could leak prompt data, create unsupported claims, or bypass human review expectations. | Not active yet. Treat as trusted only after a server-side, evidence-only implementation with source tracing, validation, and human approval. |
| ChatGPT/Codex | Development environment used to inspect and edit this repository; project workflow requires audits after pushes. | OpenAI. | Yes, through local terminal/tool actions when directed or approved. | Workspace files, docs, source code, terminal output, and any secrets accidentally present in accessible files. | Yes, can write files inside the workspace and update docs or code. | Malicious or mistaken edits could introduce insecure code, leak secrets, modify deliverables, or mix unrelated project content. | Yes, conditionally. Keep secrets out of the repo, review diffs, avoid app changes for documentation tasks, and run the required security audit after pushes. |
